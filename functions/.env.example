# Template for functions/.env.local — safe to commit.
# Copy to .env.local and fill in real values from:
#   npx web-push generate-vapid-keys

# This file is COMMITTED. Never put real key material here — only placeholders.

VAPID_PUBLIC_KEY=your-public-key-here
VAPID_PRIVATE_KEY=your-private-key-here
VAPID_SUBJECT=mailto:you@example.com
PUSH_ADMIN_TOKEN=random-hex-token-here
